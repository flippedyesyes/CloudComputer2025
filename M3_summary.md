下面是一份**可直接提交/粘到报告里的《M3 完成总结》**，**风格、颗粒度与现有 M1 / M2 总结一致**，并且**严格对齐命题二的评分点**（尤其是“小灶是否有针对性”“注重容错/Check layer”“演示闭环”）。
你不需要再加工，直接用即可。

---

# M3 Summary（学习效果评估与巩固智能体）

## 1）M3 目标与定位

在 M1（出题/判卷/错题本闭环）与 M2（知识体系树 + 掌握度回流）的基础上，
**M3 的目标是补齐“针对性纠偏与学习引导”这一核心能力**，使系统从“会评估”升级为“能辅导”。

### 对齐命题二考核指标

* **针对错题的“小灶”建议是否有针对性**
* 系统是否形成“错题 → 纠偏 → 再练 → 掌握度变化”的**闭环**
* 是否考虑 **大模型幻觉与异常输出的工程性容错（Check layer）**

M3 重点解决的问题是：

> 学生知道“错了”，但不知道“为什么错、下一步怎么练、怎么一步步想对”。

---

## 2）M3 新增核心能力概览

相较 M2，M3 新增三类关键能力：

1. **Check layer（抗幻觉闸门）**
   对所有 LLM 输出（Coach / Tutor）进行结构化校验、重试与降级，避免答非所问或直接泄露答案。

2. **Coach：错题“小灶”建议与再练闭环**
   基于错题聚合与掌握度数据，生成**可追溯证据的针对性纠偏方案**，并提供“一键再练”。

3. **Tutor：引导式对话（Hint Ladder）**
   针对单道错题，采用**多轮、分层提示**的方式引导学生自行推理，而非直接给答案。

---

## 3）Check Layer：统一的抗幻觉与容错机制（M3 基础）

### 3.1 设计动机

M3 中 Coach 与 Tutor 全部依赖 LLM 生成，若缺乏校验机制，极易出现：

* 直接给出最终答案
* 未围绕学生真实错因展开
* 输出结构漂移，导致前端或后续逻辑不可用

因此在 M3 中引入 **统一 Check layer**，作为所有 Agent 输出的“闸门”。

### 3.2 三态校验结果

每个 Checker 返回三种状态之一：

* **PASS**：通过校验，结果可入库/展示
* **RETRY**：不通过，携带原因回炉重生成（有限次数）
* **ESCALATE**：多次失败，触发安全降级逻辑（保证系统不中断）

### 3.3 已实现的 Checker

* **QuizCheck**：题目结构、难度、引用校验（承接 M1/M2）
* **GradeCheck**：判卷结构与一致性校验（承接 M1/M2）
* **CoachCheck（M3 新增）**

  * 必须引用 `missing_points / error_tags / weak_node_ids`
  * 否则视为“泛泛而谈”，直接 RETRY
* **TutorCheck（M3 新增）**

  * `hint_level < FINAL` 时禁止出现最终答案
  * 每轮只允许“一步提示 + 一个追问”

该机制显式回应了作业要求中“**注重容错、考虑幻觉**”的工程建议。

---

## 4）Coach：针对错题的“小灶”建议（命题二核心得分点）

### 4.1 错题聚合与弱点诊断

Coach 的输入并非原始文本，而是**结构化的学习画像**：

* 最近 N 天 / Top-K 错题（按 `wrong_count` 与最近错误时间）
* 聚合得到：

  * `weak_node_ids`：薄弱知识点（来自题目绑定的知识节点）
  * `error_tags`：高频错因（概念误解 / 漏条件 / 计算错误等）
  * `missing_points`：判卷阶段提取的关键缺失要点
  * `recent_mistakes_examples`：3–5 道代表性错题

### 4.2 小灶建议生成

Coach 输出固定结构化结果：

* **诊断结论**：当前主要薄弱点与错因
* **纠偏动作**：逐条对应 `missing_points` 的改进建议
* **再练计划**：围绕弱点知识点的 L1 → L2 练习路径

通过 **CoachCheck** 强制要求“建议必须可追溯到错题证据”，确保针对性。

### 4.3 一键再练闭环

Coach 输出中直接给出可执行的再练参数：

* `node_id`（薄弱知识点）
* `difficulty_mix`（偏 L1/L2）
* `num_questions`（3–5 题）

前端复用已有 `/quizzes/generate` 接口即可完成：

> 错题 → 小灶 → 再练 → 判卷 → 掌握度更新

---

## 5）Tutor：引导式对话（Hint Ladder 状态机）

### 5.1 设计目标

Tutor 的目标不是“讲解答案”，而是**模拟助教式引导**，帮助学生自己发现错误。

### 5.2 Tutor Session 状态

每个 tutor session 持久化以下最小状态：

* `question_id / stem`
* `student_answer`
* `diagnosis`（missing_points / error_tags / weak_node_ids）
* `hint_level`（L0 / L1 / L2 / FINAL）
* `turn`（轮数）
* `history`（最近多轮对话）

### 5.3 Hint Ladder 规则

* **L0**：指出可能忽略的条件/概念（不涉及解法）
* **L1**：提示关键概念，并要求学生复述或应用
* **L2**：给出一步推导或判断方向
* **FINAL**：仅在学生放弃或轮数超限时，给出完整解析

### 5.4 TutorCheck 与稳定性处理

* TutorCheck 严格禁止非 FINAL 泄露答案
* 若模型多次输出不合规：

  * 不抛 500
  * 返回安全兜底提示（仍符合 Hint Ladder）
  * 保证演示过程稳定不中断

---

## 6）前端演示路径（M3 串联效果）

在原有 M1 / M2 演示流程上，M3 仅新增 **3 个关键入口**：

1. **判卷报告页**：
   `生成小灶建议` → 展示 Coach 输出
2. **小灶卡片**：
   `一键再练` → 生成针对性新测验
3. **错题解析区**：
   `Tutor 引导` → 进入多轮 Hint Ladder 对话

完整演示闭环为：

> 故意做错 → 判卷 → 小灶建议 → 再练 → 知识树掌握度变化 + Tutor 引导

---

## 7）工程化与云原生体现（M3）

* **服务拆分**：API / Worker / Redis / Mongo 结构保持不变
* **异步任务**：Coach 与 Tutor 均在 Worker 中执行
* **持久化记忆**：错题、掌握度、Tutor session 均入库
* **容错设计**：Check layer + 重试 + 降级，避免 LLM 不稳定影响系统可用性

---

## 8）M3 总结

M3 在 M1/M2 的基础上完成了命题二中最具区分度的部分：

* 不仅“评估学习效果”，还能**针对性纠偏**
* 不仅“告诉答案”，还能**引导思考过程**
* 并通过 Check layer 将 LLM 纳入可控工程体系

至此，系统完整实现了：
**评估 → 诊断 → 纠偏 → 再练 → 巩固** 的智能学习闭环。

---

如果你愿意，我也可以**直接帮你把 M1 / M2 / M3 三个总结合并成一份最终《项目技术文档》结构**（含“架构设计 / Agent 策略 / 分工说明 / 演示脚本”），完全对齐老师的交付要求。
