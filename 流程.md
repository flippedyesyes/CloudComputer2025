# （一）：整体架构

下面给你一份“**可以真正落地实现**”的整体方案（先不纠结细节实现算法/Prompt文本），把我们讨论的：**命题二闭环 + 知识体系图 + 错题本/弱点记忆 + 难度梯度 + 判卷公平 + 引导式 Chat + Check layer + 云原生** 全部串起来。

------

# 一、你们最终要交付的系统长什么样

### 用户端（Web）

1. **首页：知识体系图（章节/知识点树）**

- 展示整份资料的章→节→知识点
- 节点颜色=掌握度（绿/黄/红），角标=主要错因（概念/计算/漏条件/审题等）
- 支持：**全局出题** / **按章节出题**

1. **测验页**

- 选择范围：全局/某章节
- 选择题量、题型比例、难度梯度（L1/L2/L3配比）
- 生成后在线作答并提交

1. **判卷报告页**

- 每题：对错、扣分点（缺失要点/错误点）、错因标签
- 解析默认“不直接给最终答案”（尤其选择题），提供“我放弃/显示答案”按钮
- 每题一个 **Chat 引导按钮（苏格拉底式）**

1. **错题本/学习计划页**

- 错题列表按 wrong_count 排序，可按章节/知识点/错因筛选
- 每题支持“一键再练（围绕弱点章节/知识点）”
- 今日复习（可选：间隔重复SRS）

------

# 二、总体架构（云原生、好讲、可部署）

### 推荐：Docker Compose 一键起（稳）

- **frontend**：React/Vue
- **api**：FastAPI/NestJS（业务接口）
- **worker**：Agent Worker（出题/判卷/教练/导师）
- **mongodb**：持久化（attempts/mistakes/mastery/knowledge_tree）
- **redis**：队列 & 缓存（任务、会话、tutor chat 状态）
- （可选）**vector store**：pgvector/milvus/chroma（资料切片检索RAG）

> “云原生点”你们就围绕：容器化 + 服务拆分 + 异步队列 + 状态存储 + 可扩展 worker 来讲。

------

# 三、核心数据与模块（把闭环跑通的最小集合）

## 1) 数据对象（简化版就够用）

- **Material**：资料元信息
- **Chunk**：资料切片（可选：向量）
- **KnowledgeNode**：知识体系节点（章/节/点）
- **Question**：题目（含 difficulty、tags/node_ids、reference、rubric）
- **Quiz**：一次测验
- **Attempt**：一次作答 + 判卷结果
- **Mistake**：错题本（student_id + question_id 唯一）
- **Mastery**：掌握度（student_id + node_id）

## 2) Agent/服务模块（按职责拆）

- **Ingest Service**：资料导入→切片→（可选向量）→生成知识体系树
- **Quiz Generator**：按范围/难度配比出题
- **Grader**：判卷（规则层 + LLM层）
- **Coach**：小灶建议 + 再练题生成
- **Tutor Chat**：引导式对话（Hint Ladder 状态机）
- **Mastery Updater**：把判卷结果回流更新知识体系图（掌握度+错因）
- **Check Layer**（独立组件）：所有 LLM 输出先校验再入库/展示

------

# 四、关键流程（你们系统的“主链路”）

## 流程 A：导入资料 → 知识体系图生成

1. 用户上传资料 → API 保存 Material
2. Worker 切片 chunks
3. Worker 生成知识体系 tree（章/节/点）并把节点和 chunk 关联
4. 首页可视化 tree（初始掌握度为灰/0.5）

**产物**：KnowledgeNode + Chunk + Material

------

## 流程 B：按全局/章节出题 → Check → 发给前端

1. 用户选择范围（全局或 node_id）+ 题量/配比
2. API 投递任务 `generate_quiz(range, difficulty_mix)` 到 Redis 队列
3. Worker 生成题目 JSON（包含 reference/rubric/tags/node_ids/source_chunks）
4. **Quiz Check Layer** 校验（结构、引用、唯一性、难度一致性）
   - PASS：入库 Question/Quiz
   - RETRY：带原因回炉重生成
5. 前端展示测验并作答

------

## 流程 C：提交答案 → 判卷公平 → 更新错题本/掌握度 → 小灶

1. 用户提交 answers → API 写 Attempt（状态 grading）
2. 投递 `grade_attempt(attempt_id)` 任务
3. Worker 判卷：
   - 客观题规则判
   - 主观题按 rubric + reference 结构化评估（可双裁判复评）
4. **Grade Check Layer**：分数/理由一致性、异常复评、证据校验
5. 写回 Attempt.grading
6. 更新 Mistake（upsert wrong_count/last_wrong_at/error_tags）
7. 更新 Mastery（节点掌握度、错因统计，向上汇总到章节）
8. Coach 生成小灶建议 & 针对性再练题（可延迟生成）
9. 前端显示判卷报告 + 小灶建议 + 一键再练

------

## 流程 D：错题解析 + 引导式 Chat（不直接给答案）

1. 判卷报告页点击 “Chat”
2. API 创建 tutor_session（题目、学生答案、诊断结果、hint_level、turn）
3. Tutor 每轮输出：**一个问题 + 可选提示（按 hint_level）**
4. **Tutor Check Layer**：防泄露答案、防跑题、必须围绕 missing_points/error_tags
5. 学生逐步发现错误；如果超过轮数且用户放弃 → 才允许 FINAL 给答案/完整解析

------

# 五、Check Layer（你们系统“抗幻觉”的统一机制）

把 Check Layer 做成所有 LLM 输出的“闸门”，统一返回三种结果：

- **PASS**：通过 → 入库/展示
- **RETRY**：不通过 → 带原因回炉重写（同一任务最多重试 N 次）
- **ESCALATE**：升级 → 触发第二裁判/降级为“证据不足/建议复习X章节”

### 你们至少要实现 4 个 checker：

1. **QuizCheck**：题目结构、答案合法、引用chunks、唯一性、难度一致性
2. **GradeCheck**：分数范围、理由与分数一致、异常复评、证据引用
3. **CoachCheck**：小灶必须引用 missing_points≥2 + 绑定 weak_tags
4. **TutorCheck**：禁止直接给答案（hint_level<FINAL）、每轮必须相关且只输出一步引导

> 这个设计你们在架构讲解里一讲，老师就知道你们认真对待“幻觉”。

------

# 六、难度梯度 & 针对性小灶（落在“可控机制”里）

## 难度梯度怎么落地到系统里

- 每题生成时明确 `difficulty_target`
- 校验器给 `difficulty_pred` 不一致则回炉
- 上线后统计 L1/L2/L3 正确率、平均耗时必须单调（你们可以用一张表呈现）

## 小灶针对性怎么保证

- 判卷必须输出：missing_points / incorrect_points / error_tags / weak_tags
- 小灶生成必须“引用这些字段”并产出：
  - 诊断（错因+弱点）
  - 纠偏动作（对应缺失要点）
  - 再练题（覆盖弱点节点，L1→L2→L3）

------

# 七、你们可以按这个节奏实现（很像真实工程推进）

## Milestone 1（先跑通闭环）

- 导入资料（先只存原文也行）
- 全局出题 → 作答 → 判卷 → 错题本入库

## Milestone 2（加分核心）

- 知识体系图生成 + 按章节出题
- Mastery 更新回流到知识图（颜色变化）

## Milestone 3（最亮眼）

- 引导式 Chat（Hint Ladder + TutorCheck 防泄答案）
- 小灶建议 + 一键再练闭环

## Milestone 4（工程加分）

- 完整 Docker Compose
- 日志、重试、失败降级（ESCALATE）

------

# 八、视频怎么讲（3–5分钟一镜到底）

1. 首页知识图 → 选第3章 → 出题
2. 故意错题 → 判卷报告（不直接给答案）
3. 点击 Chat 引导 → 学生自己发现 “不包含” 关键字
4. 回首页 → 第3章掌握度上升、错因统计变化
5. 1分钟架构：前端/API/worker/redis队列/mongo存储/Check layer闸门

------

如果你们愿意，我下一步可以把这份方案进一步“落地成工程骨架”，直接给你们：

- 项目目录结构（frontend/api/worker/checkers）
- 一套最小 API 列表（路径+入参出参）
- 关键数据 schema（Mongo collections）
- 任务队列的 job 定义（generate_quiz / grade_attempt / tutor_turn）
  这样你们就能开始分工写代码了。

# （二）具体实现流程参考

明白：你要的是**“从零开始把这个项目代码搭起来”的构建流程**（不是用户怎么点）。下面给你一套**可直接照着做的工程构建流程**，按阶段推进，每一步都能跑起来、可演示、可交付。

我用一套最稳的栈来描述：**Python + FastAPI（API） + RQ（Redis队列） + Worker（Agent） + MongoDB（持久化） + React（前端） + Docker Compose（一键启动）**。你们如果换 Node/Nest 也能按同样结构搬过去。

------

# 0) 仓库初始化与目录结构（第一天就要定死）

### 0.1 建一个单仓库（monorepo）

```
cloud-final/
  README.md
  docker-compose.yml
  .env.example

  backend/
    Dockerfile
    pyproject.toml  (或 requirements.txt)
    app/
      main.py
      api/           # 路由
      core/          # 配置、日志、鉴权(可简化)
      db/            # Mongo/Redis连接
      models/        # 数据结构定义
      services/      # 业务逻辑（不含LLM）
      agents/        # 出题/判卷/教练/导师
      checkers/      # Check layer（闸门）
      jobs/          # 队列任务定义
      utils/
    tests/

  worker/
    Dockerfile
    pyproject.toml
    worker.py        # RQ worker入口
    app/             # 复用同一套 agents/checkers/db（可用包或复制）
  
  frontend/
    Dockerfile
    package.json
    src/
      pages/
      components/
      api/
```

### 0.2 立刻写两个“跑起来的保证”

- `docker-compose.yml`：起 **frontend + backend + worker + redis + mongodb**
- `README.md`：一条命令启动 + 怎么打开网页 + 怎么走一遍 Demo

------

# 1) 先把基础设施跑通（能启动、能连库、能投队列）

## 1.1 docker-compose（最小可运行）

服务：`mongodb`、`redis`、`backend`、`worker`、`frontend`
你们第一阶段目标：执行

```bash
docker compose up --build
```

浏览器打开前端，后端 health check 返回 OK，worker 打印 “listening”。

## 1.2 后端最小骨架

- `GET /health` → 返回 `{status:"ok"}`
- `db/mongo.py`：连接 Mongo
- `db/redis.py`：连接 Redis
- `jobs/queue.py`：封装 enqueue（投递任务）

## 1.3 Worker 最小骨架

- RQ worker 监听队列：`default`
- 写一个测试 job：`jobs/ping.py`，投递后 worker 打印日志

> 到这一步，你们已经满足“工程化/云原生形态”的大头：服务拆分 + 队列 + DB + 容器化。

------

# 2) 定数据模型（这一步不做，后面全乱）

先把 Mongo collections 定下来（简化版即可）：

- `materials`：资料元信息（file、状态）
- `chunks`：切片（material_id、text、page）
- `knowledge_nodes`：知识体系树（material_id、parent_id、title、level、source_chunk_ids）
- `quizzes`：测验（student_id、node_id范围、题目列表、状态）
- `questions`：题目（quiz_id、stem、options、answer_key、rubric、reference、difficulty、node_ids、source_chunk_ids）
- `attempts`：提交（quiz_id、answers、grading结果、状态）
- `mistakes`：错题本（student_id+question_id唯一、wrong_count、last_wrong_at、error_tags）
- `mastery`：掌握度（student_id+node_id唯一、mastery_score、seen/correct/wrong、error_stats）

你们不用一次写全功能，但 **字段名要先固定**，后端/worker/前端都靠它。

------

# 3) 做“资料导入 → 切片 → 知识体系树”（你们的拓展点1核心）

## 3.1 API：导入资料

- `POST /materials`：上传 PDF/文本，创建 material（status=processing），投递 `ingest_material(material_id)`

## 3.2 Worker job：ingest_material

顺序：

1. 读取文件内容
2. 切片 chunks（先用简单规则：按段落/按页拆）
3. 生成知识体系树（先走“目录/标题抽取”最稳）
4. 写入 `chunks`、`knowledge_nodes`
5. material 状态置为 ready

## 3.3 Check layer：GraphCheck（先做简单版）

- 树无环、层级合法、节点title非空
- 失败：降级为“仅章节层级”

## 3.4 前端首页：知识体系图

- `GET /knowledge/tree?material_id&student_id`
- 先只画树（不带掌握度也行）
- 支持点击节点（选章节出题会用）

> 到这里，你们已经有一个非常漂亮的“首页亮点”可演示。

------

# 4) 做“出题（范围+难度梯度）→ 测验页面”（命题二核心1）

## 4.1 API：生成测验

- `POST /quizzes/generate`
  - body: `student_id, material_id, node_id(optional), num_questions, type_mix, difficulty_mix`
  - 创建 quiz（status=generating），投递 `generate_quiz(quiz_id)`

## 4.2 Worker job：generate_quiz

顺序：

1. 根据 node_id 确定范围 chunks（全局/章节子树）
2. 生成题目（选择+简答，按 L1/L2/L3 配比）
3. 每题必须带：`difficulty_target, node_ids, source_chunk_ids, rubric, reference_answer`
4. 写入 `questions`，更新 quiz.status=ready

## 4.3 Check layer：QuizCheck（先做最小版）

- schema齐全（stem/options/answer_key/…）
- answer_key 合法
- 引用存在（source_chunk_ids 非空）
- 难度字段在 L1/L2/L3
- 不通过 → retry 重生成（最多 N 次）

## 4.4 前端：测验页

- `GET /quizzes/{id}` 拿题目
- 提交 `POST /quizzes/{id}/submit`

------

# 5) 做“判卷（公平）→ 详解 → 错题本/掌握度更新”（命题二核心2+3）

## 5.1 API：提交答案

- 写 `attempts`（status=grading）
- 投递 job：`grade_attempt(attempt_id)`

## 5.2 Worker job：grade_attempt（两阶段判卷）

1. 客观题：规则判对错
2. 简答题：用 rubric + reference 的评估提示词输出结构化评分（Prometheus风格思路）
3. 输出 grading JSON：`score/is_correct/missing_points/incorrect_points/error_tags/feedback`

## 5.3 Check layer：GradeCheck（最小版）

- 分数范围合法
- missing_points 多但给高分 → 标记异常复评（可先做“重跑一次取中位数”）
- 解析空/字段缺失 → retry

## 5.4 更新记忆（你们“弱点记忆”）

判卷写回后，立刻做两件事：

- `mistakes`：错题 upsert（wrong_count+1、error_tags）
- `mastery`：按题目 node_ids 更新掌握度，再向上汇总章节

## 5.5 前端：判卷报告页

- 显示对错+详解+错因+缺失点
- 提供 “去错题本”“返回知识图”

> 这一阶段完成后，你们已经完整满足命题二最小闭环。

------

# 6) 做“错题本页面 + 一键再练 + 小灶建议”（考核指标之一）

## 6.1 错题本页

- `GET /mistakes?student_id&filters...`
- 默认按 wrong_count 排序
- 可按章节/错因筛选

## 6.2 小灶建议（Coach）

- 输入：最近错题聚合 + 当前题 missing_points/error_tags/weak_node_ids
- 输出：诊断 + 纠偏动作 + 再练计划（3题、L1→L2）
- Check layer：CoachCheck
  - 必须引用 ≥2 missing_points
  - 必须绑定 ≥1 weak_node/tag
  - 再练题覆盖这些节点

## 6.3 一键再练

- 从错题详情/小灶建议点 “再练”
- 实际就是调用 `/quizzes/generate`，node_id=弱点章节/知识点，难度偏基础

------

# 7) 做“引导式 Chat（不直接给答案）+ TutorCheck”（你们拓展点2核心）

## 7.1 判卷页的“Chat引导”按钮

- `POST /tutor/start`：创建 tutor_session（保存题干、学生答案、诊断、hint_level=0、turn=0）

## 7.2 `POST /tutor/next`：一轮一轮对话

- 后端把 session 发给 Tutor Agent
- Tutor 输出：一个问题 + 可选提示（按 hint ladder）
- 更新 session.turn，必要时 hint_level++

## 7.3 TutorCheck（防泄答案）

- hint_level < FINAL：禁止出现“正确答案是X/选C”
- 每轮只能输出一步（避免长篇直接讲透）
- 必须围绕 error_tags + 题干关键字（例如“不包含”）

> Demo 时非常抓眼球，也能体现你们的“Check layer”价值。

------

# 8) “难度梯度合理”的实现闭环（考核指标之一）

你们代码里要做三件事就够：

1. 出题时强制配比 L1/L2/L3
2. QuizCheck 做难度复评（不一致回炉）
3. attempt 统计正确率/耗时（简单记录），报表页展示：
   - correct_rate(L1) > correct_rate(L2) > correct_rate(L3)

不需要复杂IRT，**有单调性证据**就很好评分。

------

# 9) 项目构建顺序建议（你们照这个排期最稳）

**M1（先能跑闭环）**

- Compose 起服务
- 导入资料（先文本也行）→ 全局出题 → 提交 → 判卷 → 错题本入库

**M2（加分核心）**

- 知识体系图（章节树）+ 按章节出题
- mastery 回流变色（红黄绿）

**M3（最亮眼）**

- 小灶建议 + 一键再练
- 引导式 Chat + TutorCheck

**M4（工程加分）**

- 完整 README、日志、失败重试、最小测试
- 演示脚本和架构图

------

# 10) 你们“构建代码”的实际工作方式（团队协作）

- 先建 `main` + `dev` 两分支
- 每个模块一个 feature 分支：`feat/ingest`、`feat/quiz`、`feat/grading`、`feat/tutor`
- 每个 PR 都要求：
  - 能在 compose 下跑通基本路径
  - 加最小接口测试（哪怕 1–2 条）

------

如果你们认可这套构建流程，我可以下一步直接给你们一个“开工包”：

- `docker-compose.yml` 模板
- FastAPI 路由清单（接口名/入参/出参）
- RQ jobs 清单（ingest/generate/grade/coach/tutor）
- Mongo collections 字段模板（你们直接复制进代码）

你们照着填实现细节就能很快出一个能演示的版本。

# （三）：会议纪要
已帮你记录好《学习系统项目讨论》,详细内容请点击下方查看:
学习系统项目讨论
会议讨论了学习系统项目的功能设计、实现方法及相关注意事项，具体如下：
- **项目功能点确定**：
  - **基础功能**：包括出题、判题和错题本巩固。出题和判题需由不同主体完成，错题本可存储错题，还可引入agent根据错题给出新建议。
  - **扩展功能**：一是错误的多角度分析，不仅给出解析，还分析错因，提供个性化解释；二是整体的知识体系，构建全局知识图谱，让用户可全局或局部选择出题，引导学习链路。
- **RAG技术应用**：
  - **作用理解**：RAG可对外部知识进行切分和总结，为大模型提供更易理解的提示词，在项目中起到记忆功能，读取历史数据。
  - **实现疑问**：部分成员对RAG不太熟悉，不清楚如何让其记住数据库内容，也不确定实现难度。
- **出题相关问题**：
  - **出题自主性**：考虑给用户足够自主权，可全局或局部出题，但不能限制太死，知识图谱应类似目录，不过于细化。
  - **出题依据**：对于根据资料自动生成选择题，需明确用户是否指定知识点，以更好满足学完新知识后客观评估的需求。
- **反馈呈现形式**：
  - **直接指出与引导式**：讨论了直接指出用户错在哪和引导式对话两种反馈方式，认为方向一致，可按需选择呈现形式。
  - **辅助聊天区**：可在知识点旁设置辅助聊天区，但调用需有针对性，要喂入之前的信息。
- **项目实现与分工**：
  - **功能实现**：认为错误多角度分析和知识体系这两个点较复杂，若做好其中一点可能就很出彩，后续可简单进行功能堆积。
  - **代码与平台**：代码可能需本地运行后挂载到云上，对GitLab、Docker、MongoDB等工具使用存在疑问，分工文档需重新考虑。
  - **时间安排**：项目后期至少预留两三天自由发挥时间，考完试可能还需近一周完成。
- **其他注意事项**：
  - **抗幻觉检查**：项目涉及抗幻觉检查，类似高考检查加分情况，可在本地完成。
  - **账号与权限**：涉及账号申请、邮箱认证、仓库提交权限等问题，提交内容会计算贡献。